.repo-name
  %span {{ repo.full_github_name }}
.settings
  %a{ "ng-href": "/owners/{{ repo.owner_name }}" } Edit
.repo-activation-toggle{ "ng-class": "{ 'repo-activation-toggle--private': repo.private }" }
  %span.repo-private-label Private - $12
  %button.repo-toggle{ "ng-click": "toggle()",
    "ng-disabled": "processing",
    "ng-if": "repo.admin" }
  .repo-restricted{ "ng-if": "!repo.admin" }
    = t("cannot_activate_repo")
